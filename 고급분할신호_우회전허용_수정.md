# 고급분할신호 적신호시 우회전 허용 항상 활성화 수정

## 수정 내용

고급분할신호(SplitPhasingAdvancedObsolete)를 사용하는 교차로에서 적신호시 우회전 허용 로직을 항상 활성화하도록 수정했습니다.

## 수정된 파일

- `TrafficLightsEnhancement/Systems/TrafficLightSystems/Initialisation/CustomPhaseProcessor.cs`

## 수정 내용 상세

### 변경 사항

`CustomPhaseProcessor.ProcessLanes` 함수에서 고급분할신호 패턴일 때, CustomPhaseData 생성 후에 적신호시 우회전 허용 로직을 자동으로 적용하도록 추가했습니다.

#### 주요 로직

1. **보행자 그룹 마스크 계산**: 보행자 신호가 있는 그룹을 식별합니다.

2. **우회전 레인 감지**: 
   - 좌측 통행(LHT): 좌회전 레인
   - 우측 통행(RHT): 우회전 레인

3. **적신호시 우회전 허용 그룹 계산**:
   - 보행자 그룹을 제외한 모든 그룹
   - 현재 레인의 자체 그룹을 제외한 그룹들

4. **ExtraLaneSignal 설정**:
   - `m_YieldGroupMask`: 적신호시 양보를 허용할 그룹 마스크
   - `m_IgnorePriorityGroupMask`: 우선순위 무시 그룹 마스크
   - 두 값을 동일하게 설정하여 다른 그룹이 활성화될 때 우회전이 가능하도록 함

## 작동 방식

1. 고급분할신호로 설정된 교차로가 초기화될 때
2. CustomPhaseProcessor가 레인 그룹을 설정한 후
3. 자동으로 우회전 레인에 대해 `ExtraLaneSignal`을 설정
4. 적신호 상태에서도 다른 그룹이 활성화되면 우회전이 가능(Yield 신호)

## 효과

- 고급분할신호에서 사용자가 별도로 설정하지 않아도 적신호시 우회전이 항상 허용됩니다.
- 교통 흐름이 개선되어 대기 시간이 단축됩니다.
- 우회전 차량이 적신호에서도 양보 후 우회전할 수 있습니다.

## 참고 사항

- 이 기능은 고급분할신호 패턴에서만 자동으로 활성화됩니다.
- 다른 패턴(바닐라, 분할신호 등)에서는 기존처럼 "적신호시 우회전 허용" 옵션을 별도로 설정해야 합니다.
- 우회전 시 다른 차량과 보행자에게 양보해야 합니다.

## 테스트 권장 사항

1. 고급분할신호로 설정된 교차로에서 우회전 레인이 적신호 상태인지 확인
2. 다른 그룹이 활성화될 때 우회전 레인이 Yield 신호로 변경되는지 확인
3. 우회전 차량이 적신호에서 양보 후 우회전할 수 있는지 확인
















