# 신호등 작동 문제 분석

## 문제 상황
일부 신호등이 작동하지 않는 경우

## 주요 원인 분석

### 1. CustomPhaseData 버퍼 문제
**위치**: `PatchedTrafficLightInitializationSystem.cs` (초기화 시스템)

**문제점**:
- 교차로에 `CustomPhaseData` 버퍼가 제대로 생성되지 않았거나 비어있음
- 버퍼의 길이가 0이면 `GetNextSignalGroup`이 0을 반환하여 신호가 작동하지 않음

**확인 방법**:
- 초기화 시스템에서 `CustomPhaseData` 버퍼가 생성되는지 확인
- 버퍼의 길이가 0보다 큰지 확인
- 각 페이즈 데이터가 올바르게 설정되었는지 확인

### 2. GetNextSignalGroup 함수 문제
**위치**: `CustomStateMachine.cs`

**문제점**:
- `GetNextSignalGroup` 함수가 0을 반환하면 신호가 작동하지 않음
- 코드에서 다음 로직을 확인:
  ```csharp
  if (trafficLights.m_NextSignalGroup <= 0)
  {
      trafficLights.m_State = TrafficLightState.None; // roll a new group
      return true;
  }
  ```

**원인**:
- `customPhaseDataBuffer.Length`가 0인 경우
- 모든 페이즈의 우선순위가 0 이하인 경우
- 수동 신호 그룹이 잘못 설정된 경우

### 3. 상태 머신 전환 문제
**위치**: `CustomStateMachine.cs`

**문제점**:
- 신호등 상태가 `None` → `Beginning` → 다시 `None`으로 계속 순환
- `NextSignalGroup`이 0이면 `Beginning` 상태에서 `None`으로 돌아감

**확인해야 할 상태**:
- `TrafficLightState.None`: 초기 상태
- `TrafficLightState.Beginning`: 다음 그룹 준비 중
- `TrafficLightState.Ongoing`: 현재 신호 작동 중

### 4. 초기화 시스템 문제
**위치**: `PatchedTrafficLightInitializationSystem.cs`

**문제점**:
- 교차로 초기화 시 `CustomTrafficLights` 컴포넌트가 생성되지 않음
- 레인 그룹 분석이 실패하여 페이즈 데이터가 생성되지 않음
- 특정 교차로 타입(3-way, 4-way 외)에서 제대로 작동하지 않음

### 5. 컴포넌트 누락 문제
**확인해야 할 컴포넌트**:
- `CustomTrafficLights`: 커스텀 신호등 설정 저장
- `CustomPhaseData` 버퍼: 각 페이즈의 데이터
- `TrafficLights`: 기본 게임 신호등 컴포넌트

## 디버깅 방법

### 1. 로그 확인
`Mod.cs`에서 로그를 확인하여 다음을 체크:
- 초기화 시 에러 메시지
- `CustomPhaseData` 버퍼 생성 실패
- `GetNextSignalGroup`이 0을 반환하는 경우

### 2. 코드 체크리스트
- [ ] `PatchedTrafficLightInitializationSystem`에서 교차로 초기화 시 `CustomTrafficLights` 컴포넌트가 추가되는가?
- [ ] `CustomPhaseData` 버퍼가 생성되고 길이가 0보다 큰가?
- [ ] 각 페이즈 데이터의 `m_Priority`가 올바르게 설정되어 있는가?
- [ ] `GetNextSignalGroup` 함수가 올바른 그룹 번호를 반환하는가?
- [ ] 상태 머신이 정상적으로 전환되는가?

### 3. 일반적인 수정 사항 확인
수정한 코드에서 확인해야 할 부분:
1. 초기화 로직을 변경했는가? → `CustomPhaseData` 버퍼 생성 로직 확인
2. 상태 머신 로직을 변경했는가? → 상태 전환 조건 확인
3. 컴포넌트 추가/제거 로직을 변경했는가? → 컴포넌트 누락 확인
4. 패턴 처리 로직을 변경했는가? → `PredefinedPatternsProcessor` 확인

## 일반적인 해결 방법

### 방법 1: 초기화 재확인
초기화 시스템에서 모든 교차로에 대해:
1. `CustomTrafficLights` 컴포넌트 추가
2. `CustomPhaseData` 버퍼 생성
3. 각 페이즈에 대한 데이터 설정

### 방법 2: GetNextSignalGroup 보호 코드 추가
```csharp
if (customPhaseDataBuffer.Length == 0)
{
    // 버퍼가 비어있으면 기본 처리
    return 0;
}
```

### 방법 3: 상태 머신 안전장치 추가
`Beginning` 상태에서 `NextSignalGroup`이 0인 경우:
- 기본 신호 그룹으로 폴백
- 또는 에러 로그 출력 후 기본 동작

### 방법 4: 특정 교차로 타입 확인
GUIDE.md에 따르면 3-way, 4-way 교차로만 지원:
- 다른 타입의 교차로는 Vanilla 모드로 폴백
- 또는 해당 교차로 타입에 대한 초기화 로직 추가

## 질문할 사항

1. 어떤 교차로에서 신호가 작동하지 않는가?
   - 3-way, 4-way, 또는 다른 타입?
   
2. 어떤 패턴을 사용했는가?
   - Vanilla, Split-Phasing, Protected Turns 등?
   
3. 원본 모드와 어떻게 수정했는가?
   - 초기화 로직 변경?
   - 상태 머신 로직 변경?
   - 컴포넌트 구조 변경?

4. 게임 버전은 무엇인가?
   - 모드는 1.3.3f1용으로 개발됨

5. 특정 조건에서만 발생하는가?
   - 모든 교차로 또는 특정 교차로만?
   - 특정 설정 시에만?
















