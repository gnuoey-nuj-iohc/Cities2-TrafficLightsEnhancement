# 성능 병목 분석 및 최적화

## 발견된 성능 문제

### 1. CalculatePriority와 CalculateFlow가 매 업데이트마다 실행
**문제점**:
- 모든 교차로에 대해 매 업데이트(약 0.27초마다) 실행
- `foreach (var subLane in subLaneBuffer)` 루프가 모든 레인을 순회
- 교차로가 많을수록 계산량이 기하급수적으로 증가

**최적화**:
- 계산을 4틱마다만 실행하도록 변경 (약 1초마다)
- 상태가 변경될 때는 즉시 계산하여 정확성 유지

### 2. 그린웨이브 계산이 항상 실행
**문제점**:
- 단일 페이즈 교차로에서도 그린웨이브 계산 실행
- 인접 교차로 체크가 매번 발생

**최적화**:
- 다중 페이즈 교차로에서만 그린웨이브 계산 실행
- 단일 페이즈는 그린웨이브가 불필요하므로 스킵

### 3. 업데이트 빈도
**현재**: 4 (16프레임마다, 약 0.27초)
**옵션**: 필요시 8 또는 16으로 증가 가능 (더 나은 성능, 약간 낮은 반응성)

## 최적화 내용

### 1. 주기적 계산으로 변경
```csharp
// 매 업데이트마다 계산 → 4틱마다 계산
bool shouldCalculate = (trafficLights.m_Timer & 3) == 0 || 
                       trafficLights.m_State == TrafficLightState.None || 
                       trafficLights.m_State == TrafficLightState.Beginning;
```

### 2. 그린웨이브 체크 최적화
```csharp
// 단일 페이즈 교차로에서는 스킵
bool shouldCheckGreenWave = customPhaseDataBuffer.Length > 1 && ...
```

### 3. 상태 변경 시 즉시 재계산
- 상태가 변경되면 최신 데이터를 위해 즉시 재계산
- 정확성과 성능의 균형 유지

## 예상 성능 개선

1. **CPU 사용량 감소**: 계산 빈도가 약 75% 감소 (4틱마다)
2. **메모리 할당 감소**: 그린웨이브 계산이 단일 페이즈에서 스킵
3. **프레임 레이트 향상**: 특히 교차로가 많은 도시에서 개선

## 추가 최적화 옵션

### UpdateInterval 증가 (필요시)
```csharp
// PatchedTrafficLightSystem.GetUpdateInterval에서
return 8; // 또는 16 (더 나은 성능, 약간 낮은 반응성)
```

### 계산 주기 증가 (필요시)
```csharp
// 4틱 대신 8틱마다 계산
bool shouldCalculate = (trafficLights.m_Timer & 7) == 0 || ...
```

## 모니터링

성능이 여전히 문제라면:
1. UpdateInterval을 8로 증가
2. 계산 주기를 8틱으로 증가
3. Unity Profiler로 정확한 병목 지점 확인

## 참고

- 최적화는 신호 반응성을 최소한으로 유지하면서 수행
- 상태 변경 시에는 즉시 계산하여 정확성 보장
- 필요시 추가 최적화 가능
















