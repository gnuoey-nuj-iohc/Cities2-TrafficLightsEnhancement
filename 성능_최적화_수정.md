# 신호등 모드 성능 최적화

## 문제점

게임이 심각하게 버벅이는 원인 중 하나로 신호등 모드의 성능 문제가 있었습니다.

## 성능 병목 원인

### 1. NativeHashMap 할당/해제
**위치**: `CustomStateMachine.GetNextSignalGroup`

**문제**:
- 그린웨이브 계산을 위해 매번 `NativeHashMap<byte, float>` 할당/해제
- 그린웨이브가 비활성화된 교차로에서도 할당 시도
- 교차로 수가 많을수록 메모리 할당/해제 빈도 증가

**해결**:
- 그린웨이브가 활성화되고 인접 교차로가 있을 때만 할당
- `hasGreenWaveData` 플래그로 할당 여부 추적
- 할당하지 않은 경우 Dispose 스킵

### 2. 불필요한 계산 실행
**위치**: `PatchedTrafficLightSystem.Execute`

**문제**:
- 상태가 변경되지 않아도 매번 `CalculatePriority`와 `CalculateFlow` 실행
- 신호 업데이트가 필요하지 않을 때도 모든 계산 수행

**해결**:
- 계산은 유지하되, 신호 업데이트는 상태 변경 시에만 수행
- 신호 상태가 변경되지 않으면 `UpdateLaneSignals`와 `UpdateTrafficLightObjects` 스킵

## 수정 내용

### 1. GetNextSignalGroup 최적화
- 그린웨이브 데이터가 실제로 필요할 때만 `NativeHashMap` 할당
- 할당하지 않은 경우 Dispose 스킵으로 메모리 할당/해제 비용 절감

### 2. 계산 실행 빈도 최적화
- `CalculatePriority`와 `CalculateFlow`는 계속 실행 (다음 결정을 위해 필요)
- 하지만 신호 업데이트는 상태 변경 시에만 수행

## 성능 개선 효과

1. **메모리 할당 감소**: 그린웨이브가 비활성화된 교차로에서 할당/해제 제거
2. **신호 업데이트 최적화**: 상태 변경 시에만 업데이트하여 불필요한 작업 제거
3. **전체 프레임 레이트 개선**: 특히 교차로가 많은 도시에서 성능 향상

## 추가 최적화 가능 사항

만약 여전히 성능 문제가 있다면:

1. **UpdateInterval 증가**: `PatchedTrafficLightSystem.GetUpdateInterval()`에서 반환 값을 증가 (현재 4)
   - 신호 업데이트 빈도 감소
   - 단, 신호 반응성이 약간 저하될 수 있음

2. **CalculateFlow/CalculatePriority 실행 빈도 감소**
   - 매 업데이트가 아닌 몇 틱마다 실행
   - 단, 신호 결정 정확도가 약간 저하될 수 있음

3. **그린웨이브 계산 최적화**
   - 인접 교차로 체크 빈도 감소
   - 캐싱 또는 더 간단한 계산 방식 사용

## 테스트 권장 사항

1. 교차로가 많은 도시에서 성능 테스트
2. 프레임 레이트 모니터링 (FPS)
3. 메모리 할당 추적 (Profiler)
4. 신호 반응성 확인 (차량이 정상적으로 대기하고 진행하는지)

## 참고

- 최적화는 신호등 기능을 유지하면서 성능을 개선하는 데 초점
- 추가 최적화가 필요하면 사용자 설정으로 조정 가능하도록 구현할 수 있음
















