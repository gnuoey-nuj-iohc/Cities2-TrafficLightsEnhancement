# 신호등 작동 문제 수정 사항

## 수정 완료된 문제점

### 1. GetNextSignalGroup 함수 안전장치 추가
**파일**: `CustomStateMachine.cs`

**문제점**:
- 버퍼가 비어있을 때 또는 모든 페이즈가 선택되지 않을 때 `nextGroup`이 0으로 남아 신호가 작동하지 않음

**수정 내용**:
- 버퍼 길이가 0인 경우 즉시 0을 반환 (호출자가 처리하도록)
- `nextGroup`이 0인 경우 안전한 폴백 로직 추가:
  - 우선순위가 있거나 차량이 점유된 첫 번째 페이즈 선택
  - 그래도 없으면 첫 번째 페이즈로 기본값 설정

### 2. CustomStateMachine 상태 전환 안전장치 추가
**파일**: `CustomStateMachine.cs`

**문제점**:
- `None` 상태에서 `Beginning`으로 전환할 때 버퍼가 비어있으면 오류 발생
- `Beginning` 상태에서 `NextSignalGroup`이 0이면 무한 루프 발생

**수정 내용**:
- `None` 상태 전환 시 버퍼가 비어있으면 `false` 반환하여 바닐라 시스템으로 폴백
- `Beginning` 상태에서 버퍼 검증 추가
- `NextSignalGroup`이 유효하지 않으면 다시 계산 시도
- 여전히 유효하지 않으면 `None` 상태로 복귀

### 3. PatchedTrafficLightSystem 버퍼 검증 강화
**파일**: `PatchedTrafficLightSystem.cs`

**문제점**:
- `CustomStateMachine`을 사용하기 전에 버퍼가 비어있는지 확인하지 않음
- 초기화가 완료되지 않은 상태에서 CustomStateMachine 호출 시 오류 발생

**수정 내용**:
- `usesCustomStateMachine`이 `true`일 때도 버퍼 길이 검증 추가
- 버퍼가 비어있으면 바닐라 시스템으로 폴백하여 신호가 계속 작동하도록 보장

## 수정 효과

1. **안정성 향상**: 버퍼가 비어있거나 초기화가 완료되지 않은 경우에도 신호가 작동
2. **무한 루프 방지**: `Beginning` 상태에서 유효하지 않은 그룹을 감지하고 처리
3. **폴백 메커니즘**: 커스텀 시스템이 실패해도 바닐라 시스템으로 자동 전환

## 테스트 권장 사항

1. 새로 생성한 교차로에서 신호 작동 확인
2. 기존 교차로에서 신호 작동 확인
3. 패턴 변경 시 신호 작동 확인
4. Advanced Split Phasing 모드에서 신호 작동 확인

## 추가 참고사항

이 수정은 신호가 작동하지 않는 문제를 해결하지만, 근본 원인(초기화 실패 등)이 있는 경우 로그를 확인하여 추가 디버깅이 필요할 수 있습니다.
















