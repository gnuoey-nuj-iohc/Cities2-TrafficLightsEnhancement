# 신호등 한 방향 멈춤 문제 수정

## 문제 설명

고급분할신호에서 한 방향이 계속 녹색 신호를 유지하고 다른 방향이 계속 빨간 신호로 멈춰있는 문제가 발생했습니다.

## 원인 분석

1. **최대 지속 시간 초과 후에도 같은 그룹 선택**: `GetNextSignalGroup` 함수가 우선순위가 높은 그룹을 계속 선택하여, 최대 지속 시간(300 틱)을 초과해도 같은 방향이 계속 녹색 신호를 유지
2. **preferChange가 true여도 전환 실패**: `preferChange`가 `true`가 되어도 `nextGroup`이 현재 그룹과 같으면 신호가 전환되지 않음
3. **현재 그룹에 대한 페널티 부족**: 현재 활성화된 그룹이 계속 선택될 가능성이 높음

## 수정 내용

### 1. 최대 지속 시간 초과 시 강제 전환 로직 추가
**위치**: `CustomStateMachine.cs` - `UpdateTrafficLightState` 함수의 `Ongoing` 상태 처리 부분

**변경 사항**:
- 최대 지속 시간(`m_MaximumDuration`)을 초과하면 `forceChange` 플래그를 설정
- `nextGroup`이 현재 그룹과 같으면 다른 그룹을 강제로 찾아서 선택
- 다른 그룹 중 우선순위가 높거나 대기 시간이 긴 그룹을 선택
- 그래도 없으면 다음 그룹으로 순환

```csharp
// Force change if maximum duration exceeded, even if nextGroup is the same
bool forceChange = false;
if (customTrafficLights.m_Timer >= phase.m_MaximumDuration)
{
    forceChange = true;
    // If nextGroup is the same, find the next available group
    if (nextGroup == trafficLights.m_CurrentSignalGroup)
    {
        // Find the next group with highest priority or longest waiting time
        // ... (다른 그룹 찾기 로직)
    }
}
```

### 2. GetNextSignalGroup에서 현재 그룹 페널티 추가
**위치**: `CustomStateMachine.cs` - `GetNextSignalGroup` 함수

**변경 사항**:
- 현재 활성화된 그룹(`m_TurnsSinceLastRun == 0`)에 대해 우선순위 페널티 적용
- 다른 그룹이 선택될 확률을 높여 신호 전환을 촉진

```csharp
// Penalize current group if it's been active (indicated by m_TurnsSinceLastRun == 0)
float priorityPenalty = 0f;
if (currentGroup > 0 && phaseGroup == currentGroup && phase.m_TurnsSinceLastRun == 0)
{
    // Reduce priority for current group to allow others to have a chance
    priorityPenalty = 1f; // Subtract 1 from priority
}
```

## 효과

1. **최대 지속 시간 보장**: 각 방향은 최대 지속 시간(기본 300 틱, 약 30초)을 넘기지 않고 반드시 전환됩니다.
2. **공정한 신호 순환**: 한 방향이 다른 방향을 계속 막는 문제가 해결됩니다.
3. **교통 흐름 개선**: 모든 방향의 차량이 적절히 신호를 받을 수 있습니다.

## 주의사항

- 최대 지속 시간(`m_MaximumDuration`)이 너무 짧으면 신호가 너무 자주 전환될 수 있습니다.
- 기본값은 300 틱(약 30초)이며, 필요에 따라 조정 가능합니다.
- 우선순위 페널티는 작지만 다른 그룹이 선택될 확률을 높입니다.

## 테스트 권장 사항

1. 고급분할신호로 설정된 교차로에서 각 방향이 순차적으로 녹색 신호를 받는지 확인
2. 한 방향에 차량이 많이 있어도 최대 지속 시간 후 전환되는지 확인
3. 신호 전환이 너무 빈번하지 않은지 확인
















